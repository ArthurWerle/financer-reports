<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - {{month}}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #2c3e50;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 5px 5px 0 0;
    }
    .content {
      padding: 20px;
      background: #f9f9f9;
      border: 1px solid #ddd;
    }
    .section {
      margin-bottom: 30px;
      padding-bottom: 20px;
      border-bottom: 1px solid #eee;
    }
    .section-title {
      color: #2c3e50;
      margin-bottom: 15px;
    }
    .summary-box {
      background: #fff;
      border: 1px solid #ddd;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 20px;
    }
    .category-item {
      display: flex;
      justify-content: space-between;
      gap: 5px;
      padding: 8px 0;
      border-bottom: 1px solid #eee;
    }
    .footer {
      text-align: center;
      padding: 20px;
      font-size: 12px;
      color: #777;
    }
    .big-number {
      font-size: 24px;
      font-weight: bold;
      margin: 10px 0;
    }
    .label {
      font-size: 14px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>{{title}}</h1>
    <h2>{{month}}</h2>
  </div>
  
  <div class="content">
    <!-- Summary Section -->
    <div class="section">
      <h2 class="section-title">Monthly Summary</h2>
      <div style="display: flex; justify-content: space-between; flex-wrap: wrap;">
        <div class="summary-box" style="flex: 1; min-width: 200px; margin-right: 10px;">
          <div class="label">Total Income</div>
          <div class="big-number">R$ {{income.total}}</div>
        </div>
        <div class="summary-box" style="flex: 1; min-width: 200px; margin-right: 10px;">
          <div class="label">Total Expenses</div>
          <div class="big-number">R$ {{spending.total}}</div>
        </div>
        <div class="summary-box" style="flex: 1; min-width: 200px;">
          <div class="label">Savings Rate</div>
          <div class="big-number">{{savings.rate}}%</div>
        </div>
      </div>
    </div>
    
    <!-- Top Spending Categories -->
    <div class="section">
      <h2 class="section-title">Top Spending Categories</h2>
      <div class="summary-box">
        {{#each spending.categories}}
          <div class="category-item">
            <span>{{name}}</span>
            <span>R$ {{amount}}</span>
          </div>
        {{/each}}
      </div>
    </div>
  </div>
  
  <div class="footer">
    <p>This is an automated monthly financial report.</p>
    <p>Your personal financial assistant</p>
  </div>
</body>
</html>